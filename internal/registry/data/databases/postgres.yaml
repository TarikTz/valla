id: postgres
name: PostgreSQL
type: database
default_port: 5432
docker:
  image: postgres:16-alpine
  volume_path: /var/lib/postgresql/data
  env_vars:
    POSTGRES_USER: "{{.User}}"
    POSTGRES_PASSWORD: "{{.Password}}"
    POSTGRES_DB: "{{.Name}}"
