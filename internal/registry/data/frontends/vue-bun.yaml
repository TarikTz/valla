id: vue-bun
name: Vue
type: frontend
runtime: bun
group: Vue
requires_runtime: bun
scaffold_cmd: "bunx create-vue@latest {{.ScaffoldName}} --default"
default_port: 5173
http_client_patch:
  file: src/api.ts
  mode: create
  template: |
    export const API_BASE = import.meta.env.VITE_API_URL ?? 'http://localhost:{{.BackendPort}}'
docker:
  image: oven/bun:1
  build_context: ./frontend
  dockerfile: internal/registry/data/dockerfiles/bun-frontend.Dockerfile
  env_vars:
    VITE_API_URL: "http://localhost:{{.BackendPort}}"
