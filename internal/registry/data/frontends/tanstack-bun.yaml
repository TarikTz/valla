id: tanstack-bun
name: TanStack Start
type: frontend
runtime: bun
group: TanStack Start
requires_runtime: bun
scaffold_cmd: "bunx create-tsrouter-app@latest {{.ScaffoldName}} --framework react --template start-basic --no-git"
default_port: 3000
http_client_patch:
  file: app/api.ts
  mode: create
  template: |
    export const API_BASE = import.meta.env.VITE_API_URL ?? 'http://localhost:{{.BackendPort}}'
docker:
  image: oven/bun:1
  build_context: ./frontend
  dockerfile: internal/registry/data/dockerfiles/bun-tanstack.Dockerfile
  env_vars:
    VITE_API_URL: "http://localhost:{{.BackendPort}}"
